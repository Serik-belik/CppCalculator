#pragma once

void PrintMenu();

bool IsAllowedOperator(char opr);

bool IsUnaryOperator(char opr);

int Factorial(int n);

int Calculate(int first, int second, char opr);

float Calculate(float firstVal, float secondVal, char opr);
